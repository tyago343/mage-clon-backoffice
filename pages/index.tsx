import type { NextPage } from 'next'
import Head from 'next/head'
import Image from 'next/image'
import Sidebar from '../components/Sidebar'

const Home: NextPage = () => {
  return (
    <div className="">
        <Sidebar />
        {/* Main */}
          {/* Wrapper (top and bottom) */}
          {/* Center */}
    </div>
  )
}

export default Home
